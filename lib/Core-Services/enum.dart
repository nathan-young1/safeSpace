
//All the enum used in this application
enum Options {delete}
enum TaskDialog {upload,download}
enum ProgressDialogVisiblity{show,hide}
enum FileEncrypt{file,filePath}
enum FileDecryptType{normal,vaultReEncrypt}
enum PopupMenuChoice { edit, delete }
enum UploadFileToFirestore {fromAttachment,fromOther}
enum DeleteOption{file,document}
enum ReEncryptDataType{passwords,payments,passports,documents,certificates}
enum VaultReEncryptionMode{Normal,Resume}
enum Cyptography{Normal,ReEncryption}
enum Authentication{Successful,InvalidDetail,WrongPassword,Error,WeakPassword,EmailExists}